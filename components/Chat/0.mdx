import { Answer, Question, Writing } from './Bubbles';

<CH.Scrollycoding>

<Question>
  A react component for a chat bubble, with the little triangle pointing left or
  right
</Question>

<Answer>
  Creating a React chat bubble component with a little triangle pointing left or
  right can be achieved using both CSS and React JSX. First, let's create a
  simple CSS file to style our chat bubble
</Answer>

```js app.js focus=3:10
const { lorem, ipsum } = dolor({
  sit: {
    amet: 1,
    consectetur: 2,
    // mark(1:3)
    adipiscing: (elit) => ({
      sed: elit,
    }),
    eiusmod: (tempor) => ({
      incididunt: tempor,
    }),
    ut: (labore) => ({
      et: labore,
      dolore: labore + 1,
    }),
    magna: (aliqua) => ({
      ut: aliqua,
    }),
    nostrud: (elit) => ({
      exercitation: elit,
      ullamco: elit,
    }),
    laboris: (elit) => ({
      nisi: elit,
    }),
  },
});
```

---

<Answer>

Creating a React chat bubble component with a little triangle pointing left or
right can be achieved using both CSS and React JSX. First, let's create a
simple CSS file to style our chat bubble.

Creating a React chat bubble component with a little triangle pointing left or
right can be achieved using both CSS and React JSX. First, let's create a
simple CSS file to style our chat bubble.

</Answer>

```js app.js focus=11:17

```

---

<Answer>
  Creating a React chat bubble component with a little triangle pointing left or
  right can be achieved using both CSS and React JSX. First, let's create a
  simple CSS file to style our chat bubble
</Answer>

```js app.js focus=11:14
const { lorem, ipsum } = dolor({
  sit: {
    amet: 1,
    consectetur: 2,
    adipiscing: (elit) => ({
      sed: elit,
    }),
    eiusmod: (tempor) => ({
      incididunt: tempor,
    }),
    ut: (labore) => ({
      et: lorem(labore * ipsum),
      dolore: lorem(labore + 1),
    }),
    nostrud: (elit) => ({
      exercitation: elit,
      ullamco: elit,
    }),
    laboris: (elit) => ({
      nisi: elit,
    }),
  },
});
```

---

<Question>
  A react component for a chat bubble, with the little triangle pointing left or
  right
</Question>

<Answer>
  Creating a React chat bubble component with a little triangle pointing left or
  right can be achieved using both CSS and React JSX. First, let's create a
  simple CSS file to style our chat bubble
</Answer>

```js app.js focus=15:21

```

---

<Question>
  A react component for a chat bubble, with the little triangle pointing left or
  right
</Question>

<Answer>
  <Writing />
</Answer>

```js app.js

```

</CH.Scrollycoding>
